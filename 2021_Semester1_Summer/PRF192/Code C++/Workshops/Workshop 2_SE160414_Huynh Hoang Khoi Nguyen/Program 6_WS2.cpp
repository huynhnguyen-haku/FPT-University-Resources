/*The ASCII code table*/
#include<stdio.h>
int main ()
{ int code;
  for (code=0; code < 256; code ++)
  { printf("%c : %d, %o, %X\n", code, code, code, code);
  if (code !=0, code %20==0)
  getchar ();
  /*code page of 20 lines*/
  }
 } 
